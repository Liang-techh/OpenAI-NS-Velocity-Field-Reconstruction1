# 实际执行记录 / Native MATLAB verification

本轮把 ST054 冻结场迁移为原生 MATLAB 交互查看器，没有训练新候选。
固定数学来源：PR #630，提交 `c77492a48e9c0f13d4d51244987c57c28519409b`。
查看器任务 #685，集成 PR #689。

## 已执行

GitHub Actions 运行 **35462841391**，作业 **105949683201**，实际检出查看器源提交
`dab13c557e9811603e9d163072f461b4a4bf7a46`，使用 Ubuntu22.04.5、MATLAB **R2024b Update9**。
所有步骤 completed/success，源数据导出后执行：

```matlab
ns_selftest(file,true,out);
ns_callback_test(file,out);
```

两候选 × 96空间点 × 5时刻，与原 Python 求值器的最大绝对差：

| 量 | 最大绝对误差 |
|---|---:|
| 速度各分量 | 1.1585177261963508e-13 |
| 压力 | 8.9650509238481391e-14 |
| 完整动量残差各分量 | 1.9979148543902703e-12 |

预定检查阈值 `1e-8` 未因失败重跑而放宽。另实际检查：笛卡尔/体网格一致性、紧支撑、
散度恒等式、旋转对称、压力梯度和涡量的独立差分、窗口外输入拒绝、ST052布局适配器。
GUI 检查实际切换时间/候选/流线与表面/切片诊断，导出了本目录 `tests/output/matlab_explorer.png`。
回调测试调用实际拖动和松开回调，并启动/暂停播放定时器，确认时间变化；不声称模拟了人手拖动。

原生输出收据在 `tests/output/matlab_test_receipt.json` 和 `callback_receipt.json`。
实际运行返回的整个数据/源/截图归档为 Actions artifact **10590641704**，16文件，ZIP SHA256：
`a9151ecfa308ff3eff762f2dc4fa7ca8666956c8f66c71e5d87c3ef35a431d48`。
该归档已通过连接器下载，重新检查 ZIP CRC / SHA256，并人工查看实际 MATLAB 截图。
原生运行生成的数据和收据已经提交为 `f785af05e07764db0d35ed999f9197d9a04ff915`。

`data/st054_models.mat` SHA256 为
`59d43fdc40cd4df1675c2191f85810b9351802b2f4b1e0ac25a3bccfd9914c62`。
用户交付包使用这一份**实际经 MATLAB 运行的原样 MAT 文件**。生成头时间与平台浮点差异可能导致
重新导出文件的字节不同；原候选身份、重建数组哈希和数值参考检查分别记录，不能混为一谈。

## 保留的失败记录

运行35462199684：MATLAB 已执行到 GUI截图步骤，但 exportapp 因 no-display 环境失败，
因此该次没有完整成功收据。运行35462562818：缺少 xdpyinfo，失败在显示服务检查，未执行MATLAB。
第三次安装 x11-utils/Xvfb 并显式使用 `-display :99` 后成功。没有删改这些失败日志或科学数据。

最终日常CI为 `.github/workflows/matlab-explorer.yml`，只读仓库权限，测试已提交MAT，不需要重新导出。
临时仅在本分支生成数据的写入工作流已从最终树移除。原科学PR栈没有因此合并，main默认基线未改变。

## 不能推出的结论

这证明查看器的数值迁移与受测界面操作可复现，不是完整NS动量门槛通过。
没有在用户电脑上执行，也没有测试所有MATLAB版本、图形驱动或大屏缩放配置。
设计兼容R2021a+，实际原生验证版本仅R2024b Update9。3D流线/等值面仍受空间网格分辨率限制。
本轮没有执行全部历史科研测试或Lean，没有给出源场身份、连续误差界或奇异性证明。
