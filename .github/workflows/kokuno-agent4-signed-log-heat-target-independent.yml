name: Kokuno Agent4 signed-log heat-target independent audit

on:
  push:
    branches:
      - agent-kokuno-4/signed-log-heat-target-independent-013
  pull_request:
    paths:
      - 'src/openai_ns_reconstruction/kokuno_log_rescaled_heat_repair_target.py'
      - 'src/openai_ns_reconstruction/kokuno_terminal_tail_schedule.py'
      - 'src/openai_ns_reconstruction/kokuno_outer_reserved_patch_schedule.py'
      - 'src/openai_ns_reconstruction/kokuno_independent_signed_log_heat_target_audit.py'
      - 'tests/test_constrained_kokuno_log_rescaled_heat_repair_target.py'
      - 'tests/test_constrained_kokuno_independent_signed_log_heat_target_audit.py'
      - '.github/workflows/kokuno-agent4-signed-log-heat-target-independent.yml'
  workflow_dispatch:

jobs:
  independent-audit:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Install package
        run: python -m pip install -e '.[dev]'
      - name: Run focused independent tests
        run: >-
          python -m pytest -q -W error
          tests/test_constrained_kokuno_log_rescaled_heat_repair_target.py
          tests/test_constrained_kokuno_independent_signed_log_heat_target_audit.py
      - name: Generate independent report
        run: >-
          python -m openai_ns_reconstruction.kokuno_independent_signed_log_heat_target_audit
          --output artifacts/kokuno_agent4/independent_signed_log_heat_target_audit.json
      - name: Display report
        run: cat artifacts/kokuno_agent4/independent_signed_log_heat_target_audit.json
      - name: Upload report
        uses: actions/upload-artifact@v4
        with:
          name: kokuno-agent4-independent-signed-log-heat-target-audit
          path: artifacts/kokuno_agent4/independent_signed_log_heat_target_audit.json
          if-no-files-found: error
