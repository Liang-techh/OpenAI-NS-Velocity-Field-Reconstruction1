name: kokuno-agent4-source-compatible-partition-independent

on:
  push:
    branches:
      - codex/kokuno-a4-source-compatible-partition-independent-021
  pull_request:
    branches:
      - codex/kokuno-a2-source-compatible-partition-022
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: kokuno-a4-source-compatible-partition-independent-${{ github.ref }}
  cancel-in-progress: true

jobs:
  independent-audit:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Install
        run: python -m pip install -e '.[dev]'
      - name: Focused parent and independent tests
        run: >-
          python -m pytest -q -W error
          tests/test_constrained_kokuno_source_compatible_partition.py
          tests/test_constrained_kokuno_source_compatible_partition_independent.py
      - name: Generate independent partition report
        run: |
          python -m openai_ns_reconstruction.kokuno_source_compatible_partition_independent \
            --output artifacts/kokuno_agent4/source_compatible_partition_independent_audit.json
          cat artifacts/kokuno_agent4/source_compatible_partition_independent_audit.json
      - name: Upload report
        uses: actions/upload-artifact@v4
        with:
          name: kokuno-agent4-source-compatible-partition-independent-audit
          path: artifacts/kokuno_agent4/source_compatible_partition_independent_audit.json
