name: kokuno-agent2-current-partial-composite-batch-differentials

on:
  push:
    branches:
      - codex/kokuno-a2-partial-composite-differentials-081
  pull_request:
    branches:
      - codex/kokuno-a2-current-partial-leading-oscillatory-080
  workflow_dispatch:

jobs:
  exact-head:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Check out exact A2 head
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.sha || github.sha }}
          path: a2
          fetch-depth: 0

      - name: Verify exact parent ancestry and three-file scope
        shell: bash
        run: |
          set -euo pipefail
          cd a2
          test "$(git rev-parse HEAD)" = "${{ github.event.pull_request.head.sha || github.sha }}"
          git merge-base --is-ancestor 3a6405bbd3d10b8c3b38078f0c989c45e5d407b4 HEAD
          test "$(git rev-list --count 3a6405bbd3d10b8c3b38078f0c989c45e5d407b4..HEAD)" -eq 3
          mapfile -t files < <(git diff --name-only 3a6405bbd3d10b8c3b38078f0c989c45e5d407b4..HEAD)
          test "${#files[@]}" -eq 3
          printf '%s\n' "${files[@]}" | sort > /tmp/actual-files
          printf '%s\n' \
            .github/workflows/kokuno-agent2-current-partial-composite-batch-differentials.yml \
            src/openai_ns_reconstruction/kokuno_current_partial_composite_batch_differentials.py \
            tests/test_constrained_kokuno_current_partial_composite_batch_differentials.py \
            | sort > /tmp/expected-files
          diff -u /tmp/expected-files /tmp/actual-files

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install A2 test dependencies
        shell: bash
        run: python -m pip install -e './a2[dev]'

      - name: Run focused A2 regressions
        shell: bash
        run: |
          set -euo pipefail
          PYTHONPATH="$PWD/a2/src" python -m pytest -q \
            a2/tests/test_constrained_kokuno_oscillatory_batch_differentials.py \
            a2/tests/test_constrained_kokuno_current_partial_leading_oscillatory_velocity.py \
            a2/tests/test_constrained_kokuno_current_partial_composite_batch_differentials.py

      - name: Check out exact Agent-1 Cartesian leading sibling
        uses: actions/checkout@v4
        with:
          ref: 7dc09c59584a6b1bfaabcbb8df00283d9f9a40c1
          path: a1
          fetch-depth: 1

      - name: Authenticate exact Agent-1 head and source blob
        shell: bash
        run: |
          set -euo pipefail
          test "$(git -C a1 rev-parse HEAD)" = "7dc09c59584a6b1bfaabcbb8df00283d9f9a40c1"
          test "$(git -C a1 hash-object src/openai_ns_reconstruction/kokuno_pa16_current_cartesian_leading_velocity.py)" = \
            "ca8b80b0451be1a8f31deaf620f542d8c2e92c0c"

      - name: Materialize real partial-composite differential receipt
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p receipts
          PYTHONPATH="$PWD/a1/src" python - <<'PY'
          from pathlib import Path
          import json
          import openai_ns_reconstruction

          from openai_ns_reconstruction.kokuno_pa16_current_cartesian_leading_velocity import (
              KokunoPA16CurrentCartesianLeadingVelocity,
          )
          assert KokunoPA16CurrentCartesianLeadingVelocity.__module__ == (
              "openai_ns_reconstruction.kokuno_pa16_current_cartesian_leading_velocity"
          )

          a2_pkg = str((Path("a2/src/openai_ns_reconstruction")).resolve())
          openai_ns_reconstruction.__path__.insert(0, a2_pkg)

          from openai_ns_reconstruction.kokuno_current_partial_composite_batch_differentials import (
              materialize_current_partial_composite_differentials_receipt,
          )

          receipt = materialize_current_partial_composite_differentials_receipt()
          Path("receipts/kokuno_a2_current_partial_composite_batch_differentials.json").write_text(
              json.dumps(receipt, indent=2, sort_keys=True) + "\n",
              encoding="utf-8",
          )
          PY

      - name: Upload receipt before gate enforcement
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: kokuno-a2-current-partial-composite-batch-differentials-receipt
          path: receipts/kokuno_a2_current_partial_composite_batch_differentials.json
          if-no-files-found: error

      - name: Enforce frozen mechanical gates
        shell: bash
        run: |
          set -euo pipefail
          PYTHONPATH="$PWD/a1/src" python - <<'PY'
          from pathlib import Path
          import json
          import openai_ns_reconstruction

          from openai_ns_reconstruction.kokuno_pa16_current_cartesian_leading_velocity import (
              KokunoPA16CurrentCartesianLeadingVelocity,
          )
          assert KokunoPA16CurrentCartesianLeadingVelocity.__module__ == (
              "openai_ns_reconstruction.kokuno_pa16_current_cartesian_leading_velocity"
          )
          a2_pkg = str((Path("a2/src/openai_ns_reconstruction")).resolve())
          openai_ns_reconstruction.__path__.insert(0, a2_pkg)

          from openai_ns_reconstruction.kokuno_current_partial_composite_batch_differentials import (
              enforce_receipt,
          )

          receipt = json.loads(
              Path("receipts/kokuno_a2_current_partial_composite_batch_differentials.json").read_text(
                  encoding="utf-8"
              )
          )
          enforce_receipt(receipt)
          PY
