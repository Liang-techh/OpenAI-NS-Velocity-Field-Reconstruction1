name: kokuno-agent4-current-qs-release2-independent-audit

on:
  push:
    branches:
      - kokuno-agent4/current-qs-release2-audit-128
  pull_request:
    paths:
      - "src/openai_ns_reconstruction/kokuno_a4_current_qs_release2_independent_audit.py"
      - "tests/test_constrained_kokuno_a4_current_qs_release2_independent_audit.py"
      - ".github/workflows/kokuno-agent4-current-qs-release2-independent-audit.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  independent-audit:
    runs-on: ubuntu-latest
    timeout-minutes: 90
    steps:
      - name: Checkout exact head
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.sha || github.sha }}
          fetch-depth: 0

      - name: Assert exact A1 ancestry and minimal scope
        shell: bash
        run: |
          set -euo pipefail
          parent=8503f2ede2dd604392198b31ffdeb79321822a30
          git merge-base --is-ancestor "$parent" HEAD
          test "$(git rev-list --count "$parent"..HEAD)" -eq 3
          mapfile -t changed < <(git diff --name-only "$parent"...HEAD | sort)
          printf '%s\n' "${changed[@]}"
          test "${#changed[@]}" -eq 3
          expected=(
            ".github/workflows/kokuno-agent4-current-qs-release2-independent-audit.yml"
            "src/openai_ns_reconstruction/kokuno_a4_current_qs_release2_independent_audit.py"
            "tests/test_constrained_kokuno_a4_current_qs_release2_independent_audit.py"
          )
          mapfile -t expected_sorted < <(printf '%s\n' "${expected[@]}" | sort)
          test "$(printf '%s\n' "${changed[@]}")" = "$(printf '%s\n' "${expected_sorted[@]}")"

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install package
        run: python -m pip install -e '.[dev]'

      - name: Compile A4 validator and focused regression
        run: |
          python -m py_compile src/openai_ns_reconstruction/kokuno_a4_current_qs_release2_independent_audit.py
          python -m py_compile tests/test_constrained_kokuno_a4_current_qs_release2_independent_audit.py

      - name: Materialize save-reloaded candidate and raw independent receipt
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p artifacts/kokuno_agent4_current_qs_release2_independent_audit
          python - <<'PY'
          import json
          import traceback
          from pathlib import Path
          from openai_ns_reconstruction.kokuno_a4_current_qs_release2_independent_audit import (
              TASK,
              UPSTREAM_HEAD,
              audit_loaded_current_qs_state,
              default_candidate,
          )
          from openai_ns_reconstruction.kokuno_current_exterior_qs_release2_state import (
              KokunoCurrentExteriorQsRelease2State,
          )

          root = Path("artifacts/kokuno_agent4_current_qs_release2_independent_audit")
          candidate_path = root / "candidate.json"
          report_path = root / "independent_report.json"
          candidate = default_candidate()
          candidate.save_configuration(candidate_path)
          loaded = KokunoCurrentExteriorQsRelease2State.load_configuration(candidate_path)
          try:
              report = audit_loaded_current_qs_state(loaded, candidate)
          except Exception as exc:
              report = {
                  "schema": "kokuno-a4-current-qs-release2-independent-audit-exception-v1",
                  "task": TASK,
                  "upstream_head": UPSTREAM_HEAD,
                  "candidate_semantic_sha256": loaded.semantic_sha256,
                  "audit_exception_type": type(exc).__name__,
                  "audit_exception": str(exc),
                  "traceback": traceback.format_exc(),
                  "pde_validated": False,
              }
          report_path.write_text(json.dumps(report, indent=2, sort_keys=True, allow_nan=False) + "\n")
          print(json.dumps(report, indent=2, sort_keys=True, allow_nan=False))
          PY

      - name: Upload raw candidate and independent receipt before enforcement
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: kokuno-agent4-current-qs-release2-independent-audit
          path: artifacts/kokuno_agent4_current_qs_release2_independent_audit/

      - name: Run parent and A4 focused regressions
        run: |
          python -m pytest -q -W error \
            tests/test_kokuno_current_exterior_qs_release2_state.py \
            tests/test_constrained_kokuno_a4_current_qs_release2_independent_audit.py

      - name: Enforce preregistered K4-VAL-128 gates
        shell: bash
        run: |
          set -euo pipefail
          python - <<'PY'
          import json
          from pathlib import Path
          from openai_ns_reconstruction.kokuno_a4_current_qs_release2_independent_audit import (
              enforce_preregistered_gates,
              public_api_has_no_scientific_tuning_knobs,
          )

          path = Path("artifacts/kokuno_agent4_current_qs_release2_independent_audit/independent_report.json")
          report = json.loads(path.read_text())
          if "audit_exception" in report:
              raise AssertionError(
                  f"K4-VAL-128 audit raised {report.get('audit_exception_type')}: "
                  f"{report['audit_exception']}"
              )
          enforce_preregistered_gates(report)
          assert public_api_has_no_scientific_tuning_knobs() is True
          assert report["truth_boundary"]["current_l_minus_h_matching_bridge_materialized"] is False
          assert report["truth_boundary"]["complete_ns_admission_ready"] is False
          assert report["truth_boundary"]["pde_validated"] is False
          assert report["final_project_admission_ready"] is False
          assert report["gates"]["final_project_momentum_gate_unchanged"] == 1.0e-3
          assert report["gates"]["final_project_divergence_gate_unchanged"] == 1.0e-5
          assert report["gates"]["final_project_quadrature_unchanged"] == [24, 48, 96]
          PY
